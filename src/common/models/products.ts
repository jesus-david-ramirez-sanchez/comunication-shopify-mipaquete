export interface dimensionsModel {
    product_id: number,
    height: number,
    width: number,
    length: number,
}

export interface product {
    price: number,
    height: number,
    length: number,
    width: number,
    weight: number,
    quantity: number,
}